spring:
  application:
    name: msCartoes
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://cursoms-eureka-user:lahasurviipedannilo@localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}} #1.7.3 Changing the Eureka Instance ID https://cloud.spring.io/spring-cloud-netflix/multi/multi__service_discovery_eureka_clients.html

mq:
  queues:
    emissao-cartoes: emissao-cartoes